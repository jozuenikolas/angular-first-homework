import { ArchonRedirectDirective } from './archon-redirect.directive';

describe('ArchonRedirectDirective', () => {
  it('should create an instance', () => {
    const directive = new ArchonRedirectDirective();
    expect(directive).toBeTruthy();
  });
});
